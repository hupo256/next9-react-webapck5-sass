{
  "name": "pure-next-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "test": "next test",
    "dev:build": "NEXT_PUBLIC_NODE_ENV=developmentEnv && next build && next export",
    "test:build": "NEXT_PUBLIC_NODE_ENV=testingEnv && next build && next export",
    "prod:build": "NEXT_PUBLIC_NODE_ENV=ProdEnv && next build && next export",
    "start": "next start",
    "export": "next build && next export",
    "serve:out": "yarn export && serve -p 3000 out"
  },
  "devDependencies": {
    "serve": "^11.3.2"
  },
  "dependencies": {
    "antd": "^4.15.3",
    "axios": "^0.21.1",
    "classnames": "^2.3.1",
    "next": "10.1.3",
    "react": "17.0.2",
    "react-card-carousel": "^1.1.3",
    "react-dom": "17.0.2",
    "react-viewer": "^3.2.2",
    "sass": "^1.32.12"
  }
}
