{
  "name": "pure-next-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "test": "next test",
    "dev:build": "node ./env.js DEV && next build && next export",
    "pre:build": "node ./env.js PRE && next build && next export",
    "test:build": "node ./env.js TEST && next build && next export",
    "prod:build": "node ./env.js PROD && next build && next export",
    "test1:build": "node ./env.js TEST1 && next build && next export",
    "prod1:build": "node ./env.js PROD1 && next build && next export",
    "uat:build": "node ./env.js UAT && next build && next export",
    "start": "next start",
    "build": "next build",
    "export": "next build && next export",
    "serve:out": "yarn dev:build && serve -p 3000 out"
  },
  "devDependencies": {
    "@types/react": "^17.0.5",
    "serve": "^11.3.2",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@ant-design/icons": "^4.6.2",
    "antd": "^4.15.3",
    "axios": "^0.21.1",
    "classnames": "^2.3.1",
    "next": "10.1.3",
    "react": "17.0.2",
    "react-card-carousel": "^1.1.3",
    "react-dom": "17.0.2",
    "react-viewer": "^3.2.2",
    "sass": "^1.32.13",
    "swiper": "^6.6.2"
  }
}
